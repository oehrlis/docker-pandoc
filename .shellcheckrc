# Shellcheck configuration
# Disable specific checks that may not apply to all scripts

# SC2034: Variable appears unused
disable=SC2034

# SC1091: Not following sourced files
disable=SC1091

# SC2004: $/${} is unnecessary on arithmetic variables (style)
disable=SC2004

# SC2310: Function invoked in condition (info only)
disable=SC2310

# SC2250: Prefer putting braces around variable references (style preference)
disable=SC2250

# SC2312: Consider invoking commands separately (info only)
disable=SC2312

# SC2086: Double quote to prevent globbing
# (Sometimes we want word splitting, e.g. for $PIP_ARGS)
disable=SC2086

# Enable all optional checks
enable=all
